# 🎙️ מדריך שמע מצלמה — Cardo Freecom 4x → H88+ PRO

## איך זה עובד

```
┌──────────────┐      WiFi       ┌──────────────┐
│  H88+ PRO    │◄───────────────►│   iPhone      │
│  מצלמת 360   │   (SupCam Pro)  │              │
│  📹 וידאו→SD │                 │  🎙️ שמע←BT   │
└──────────────┘                 └──────┬───────┘
                                       │ Bluetooth
                                 ┌─────┴────────┐
                                 │ Cardo Freecom │
                                 │     4x        │
                                 │  🎧 דיבורית   │
                                 └──────────────┘
```

**המצלמה** מקליטה וידאו 360 על כרטיס SD.
**האייפון** מקליט שמע מהדיבורית (דרך Bluetooth) במקביל.
**אחרי הנסיעה** — ממזגים את הוידאו + השמע בלחיצה אחת.

---

## A. הגדרה חד-פעמית

### 1. חיבור Cardo Freecom 4x לאייפון
- הדיבורית כבר מחוברת לאייפון שלך דרך Bluetooth ✓
- וודא שהיא מופיעה ב: **הגדרות → Bluetooth → Cardo Freecom**

### 2. התקנת FFmpeg במחשב Windows
- הורד את FFmpeg מ: **https://www.gyan.dev/ffmpeg/builds/**
  - בחר: `ffmpeg-release-essentials.zip`
- חלץ את הקובץ לתיקייה: `C:\ffmpeg\`
- וודא שקיים הקובץ: `C:\ffmpeg\bin\ffmpeg.exe`
- **אפשרות:** הוסף `C:\ffmpeg\bin\` ל-PATH של Windows (מתקדם)

### 3. הכנת תיקיית עבודה
- צור תיקייה במחשב, למשל: `C:\RideVideos\`
- שים בתוכה את הסקריפטים מתיקיית `scripts/` של הפרויקט

---

## B. לפני נסיעה

1. **הפעל את הדיבורית** — וודא שמחוברת לאייפון
2. **פתח את כלי בדיקת המיקרופון** באתר: `tools/mic-test.html`
   - בחר את "Cardo Freecom" ברשימת המיקרופונים
   - דבר ווידוא שרואים רמת שמע (פס ירוק זז)
   - לחץ "הקלט 5 שניות ונגן" — וודא שאתה שומע את עצמך
3. **הכן את המצלמה** — ודא שכרטיס SD פנוי ומצלמה טעונה

---

## C. בזמן נסיעה

### שלב 1: התחל וידאו
- הפעל הקלטת וידאו במצלמה (מכפתור המצלמה או מ-SupCam Pro)

### שלב 2: התחל שמע
- פתח את **מקליט הנסיעה** באתר: `tools/ride-recorder.html`
- בחר את "Cardo Freecom" כמיקרופון
- וודא שיש רמת שמע (פס ירוק)
- לחץ **🔴 התחל הקלטה**
- השאר את המסך דולק (או נעל — ההקלטה ממשיכה)

### שלב 3: סיום
- עצור את הקלטת הוידאו במצלמה
- חזור למקליט הנסיעה ולחץ **⏹ עצור הקלטה**
- הורד את קובץ השמע לאייפון

**טיפ:** נסה להתחיל את שתי ההקלטות בו-זמנית (מצלמה + שמע). ככל שהם מתחילים קרוב יותר — הסנכרון יהיה טוב יותר.

---

## D. אחרי נסיעה — מיזוג

### אפשרות 1: גרירה (הכי קל)
1. העבר את קובץ הוידאו מכרטיס ה-SD למחשב
2. העבר את קובץ השמע מהאייפון למחשב (AirDrop / כבל / ענן)
3. **גרור את שני הקבצים יחד** על `merge-audio.bat`
4. הקובץ הממוזג יישמר בתיקיית `output\`

### אפשרות 2: תיקיית input
1. שים את קובץ הוידאו (mp4/mov) בתיקיית `input\`
2. שים את קובץ השמע (webm/wav) בתיקיית `input\`
3. הפעל את `merge-audio.bat`
4. הקובץ הממוזג יישמר בתיקיית `output\`

**הוידאו לא מקודד מחדש** — המיזוג מהיר ואיכות הוידאו 360 נשמרת 1:1.

---

## E. פתרון בעיות

### "אין שמע — בדוק חיבור דיבורית"
- וודא שהדיבורית דולקת ומחוברת לאייפון
- ב-iPhone: הגדרות → Bluetooth → וודא ש-Cardo Freecom מחובר
- נסה לכבות ולהדליק Bluetooth באייפון
- רענן את דף בדיקת המיקרופון

### "לא נמצאו מיקרופונים"
- הדפדפן צריך הרשאה למיקרופון
- ב-Safari: הגדרות → Safari → מיקרופון → אפשר
- נסה דפדפן Chrome אם Safari לא עובד

### "השמע לא מסונכרן עם הוידאו"
- נסה להתחיל את שתי ההקלטות ברגע אותו (מצלמה + אייפון)
- אם יש הפרש: פתח את הקובץ בעורך וידאו (כמו CapCut) וגרור את ערוץ השמע

### "FFmpeg לא נמצא"
- וודא שהורדת FFmpeg וחילצת ל: `C:\ffmpeg\bin\ffmpeg.exe`
- או הוסף את הנתיב ל-PATH של Windows

### "שגיאה במיזוג"
- וודא שהקבצים תקינים (נסה לנגן אותם בנפרד)
- וודא שיש מספיק מקום בדיסק
